type Query {
    accounts: [Account]
}

type Account  @key(fields: "id"){
    id: ID!
    currency: Currency!
    balance: Float!
    status: String!
}

# Supported currencies
enum Currency {
    EUR, CAD, USD
}